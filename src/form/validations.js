export const nonEmptyString = /([^\s])/

export const nonEmptyCharString = /^[a-zA-Z]+$/

export const zipCodeValidator = /^[1-9][0-9]{5}$/

export const onlyNumbers = /^\d+$/

export const decimalNumbers = /^\d+(\.\d{1,2})?$/
